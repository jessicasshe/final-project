- Uploading image files in Java Swing
-> Select JFileChooser 
-> Upload using BufferedImage OR ImageIcon

- Adding image files to BLOB column type
https://www.sqlitetutorial.net/sqlite-java/jdbc-read-write-blob/

- Changing icon on the JLabel 
-> setIcon(ImageIcon object)
-> setIcon(null) first to show change?

- Insert BLOB data into SQLite database 
  1. Prepare UPDATE statement
  2. Get DB connection
  3. Read file and return byte array
 -> Implement a readFile function that returns a byte[]
-> Create byte buffer as new byte object [] ?
-> create a new ByteArrayOutputStream() object
-> Call the .read(buffer) function on the file (reading file contents for conversion)
-> Call the .write(byte [] object, 0, len) on the new bos object 
-> Remember to use exceptionhandling
-> Return using bos.toByteArray() 
  5. executeUpdate() to execute the update statement OR insert statement
  Picture gets added! 

- How to update the picture ? 

- Learned more about key Github concepts

Local vs remote branches: Work in a local branch on each members computer, remote branch exists in the SHARED server/repo
-> COMMIT: Save work with messages
-> PUSH: Commits go from Local -> Remote
-> PULL: Code pulled from remote to local 
