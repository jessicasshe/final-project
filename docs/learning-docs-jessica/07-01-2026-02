- Uploading image files in Java Swing
-> Select JFileChooser 
-> Upload using BufferedImage OR ImageIcon

- Adding image files to BLOB column type
https://www.sqlitetutorial.net/sqlite-java/jdbc-read-write-blob/

- Changing icon on the JLabel 
-> setIcon(ImageIcon object)
-> setIcon(null) first to show change?

- Learned more about key Github concepts

Local vs remote branches: Work in a local branch on each members computer, remote branch exists in the SHARED server/repo
-> COMMIT: Save work with messages
-> PUSH: Commits go from Local -> Remote
-> PULL: Code pulled from remote to local 
